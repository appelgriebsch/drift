# difft onelight theme (the built-in default on light terminals).
#
# Copy this to ~/.config/difft/config.toml and tweak to taste, or pass it
# with --config. `[colors]` is the palette; `[styles]` maps named UI
# components to a `fg bg attrs...` spec resolved against that palette. Any
# color token also accepts a literal `#rrggbb`, a 0-255 index, or an ANSI
# name (e.g. `blue`). Use `-` for "no color".

theme = "onelight"       # syntax-highlighting theme (onedark/onelight or a syntect name)
syntax = true            # highlight diff content
intraline = true         # word-level change emphasis
line_numbers = true      # old/new line-number gutter
tab_width = 4

[colors]
add         = "#50a14f"  # green
remove      = "#e45649"  # red
context     = "#383a42"  # foreground
header      = "#4078f2"  # blue
line_number = "#a0a1a7"  # grey
primary     = "#a626a4"  # purple  (logo badge background)
secondary   = "#4078f2"  # blue    (help badge, dialog border)
foreground  = "#383a42"
background  = "#fafafa"
muted       = "#a0a1a7"
surface     = "#eaeaeb"  # status bar / chip background
cursor      = "#cdd1d8"  # current-line highlight
add_line    = "#e6f3e6"  # whole-line wash for added lines
remove_line = "#fbe9e8"  # whole-line wash for removed lines
add_emph    = "#cdead0"  # intra-line changed-word background (added)
remove_emph = "#f7d3d0"  # intra-line changed-word background (removed)

# Component styles: `fg bg attrs...`. Attrs: bold, faint/dim, italic,
# underline, strikethrough, blink, reverse, conceal.
[styles]
add                = "add"
remove             = "remove"
context            = "context"
header             = "header bold"
line_number        = "line_number"
statusbar          = "foreground surface"
statusbar_logo     = "background primary bold"
statusbar_filename = "foreground surface bold"
statusbar_add      = "add surface"
statusbar_remove   = "remove surface"
statusbar_flags    = "muted surface"
statusbar_stats    = "foreground surface"
statusbar_watch    = "background add bold"
statusbar_help     = "background secondary bold"
help_key           = "muted bold"
help_desc          = "muted faint"
dialog             = "foreground background"
dialog_border      = "secondary background"
